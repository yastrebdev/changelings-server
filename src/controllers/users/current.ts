import type { RequestType } from "../../types/RecResTypes"
import type { Response } from 'express'

/**
 * @route POST /api/user/guest
 * @desc Создание гостя
 * @access Public
 */

const current = async (req: RequestType, res: Response) => {
    
};

export default current